// INSTRUCTIONS:
// Create a function that converts a string to an array.
let string = 'This is a string'
const toArr = (str) => {
  return str.split(' ')
}

console.log(toArr(string))
// The output should be an array.

// ***Bonus***
// Convert a number to a string
let num = 89 

const numToString = (n) =>{
  return n.toString()
}
//Convert an array to a stringarrls

console.log(numToString(num))
